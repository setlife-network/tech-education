import React from 'react';

import Card from 'components/Card';

const CourseTile = ({
    title,
    description,
    current_version
}) => {
    return (
        <Card>
            Course
        </Card>
    )
}

export default CourseTile