const baseSize = 10

export default {
    base:   `${baseSize}px`,
    tiny:   `${baseSize * .7}px`,
    small:  `${baseSize * .85}px`,
    medium: `${baseSize * 1.4}px`,
    large:  `${baseSize * 2}px`,
    xlarge: `${baseSize * 3}px`
}
