import React from 'react';
import { Card } from 'react-bootstrap';

const CourseTile = ({
    title,
    description,
    current_version
}) => {
    return (
        <Card body>
            Course
        </Card>
    )
}

export default CourseTile